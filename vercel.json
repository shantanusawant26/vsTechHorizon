{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "frontend/docs" }
    }
  ],
  "routes": [
    
    { "src": "/(.*)", "dest": "frontend/docs/index.html" }
  ]
}
