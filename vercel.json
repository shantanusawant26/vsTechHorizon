{
    "version": 2,
    "name": "my-angular-node-app",
    "builds": [
      

      {
        "src": "frontend",
        "use": "@vercel/static-build",
        "config": { "distDir": "frontend/docs" }
      },
      {
        "src": "backend/**/*.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "^/(.*)", "dest": "$1" }
    ]
  }
  